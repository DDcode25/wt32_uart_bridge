/*
 * uart_manager.h
 *
 * Управление тремя независимыми UART-каналами ESP32 (UART0/1/2).
 * Каждый канал: приём в отдельной FreeRTOS-задаче через кольцевой
 * буфер драйвера (uart_driver_install), передача байт наружу через
 * зарегистрированный колбэк (routing_manager подписывается на RX,
 * а сеть/протокол вызывает uart_manager_write() на TX).
 *
 * ISR ничего не парсит — только приёмный буфер драйвера ESP-IDF.
 * Разбор протокола происходит в задаче канала (protocol_*).
 */
#pragma once

#include <stdint.h>
#include <stdbool.h>
#include "driver/uart.h"
#include "esp_err.h"

#ifdef __cplusplus
extern "C" {
#endif

#define UART_MGR_NUM_CHANNELS   3
#define UART_MGR_MAX_NAME_LEN   24
#define UART_MGR_RX_RING_BYTES  2048
#define UART_MGR_TX_RING_BYTES  2048

/* Порция, которой задача приёма забирает данные из драйвера.
 *
 * Именно она, а не таймаут, задаёт задержку транзита. uart_read_bytes()
 * передаёт ticks_to_wait заново на КАЖДОЙ итерации своего внутреннего
 * цикла, не уменьшая его, поэтому таймаут означает "сколько ждать
 * очередной порции", а не "сколько ждать всего". На непрерывном потоке
 * пауз нужной длины не бывает, и вызов возвращается только набрав
 * запрошенное — то есть задержка равна времени накопления полной порции.
 *
 * При CRSF 400000 бод реальный поток около 6.8 КБ/с: 512 байт копились
 * бы ~75 мс, что для команд управления недопустимо. 64 байта дают меньше
 * 10 мс и около 107 датаграмм в секунду — для Ethernet это ничто.
 * Разреженные каналы от мелкой порции не страдают: там поток прерывается,
 * таймаут отрабатывает и отдаёт накопленное. */
#define UART_MGR_RX_CHUNK_BYTES 64
#define UART_MGR_RX_WAIT_MS     20

/* Очередь передачи, по одной на канал.
 *
 * Запись в UART НЕ ДОЛЖНА выполняться в задаче, которая принимает данные:
 * однопроводный режим ждёт реального ухода посылки, и на живом RC-потоке
 * (около 105 датаграмм в секунду) приёмная задача UDP простаивала в этом
 * ожидании ~110 мс на посылку. Пакеты копились в буфере сокета и молча
 * терялись — до провода доходило 9 из 105 в секунду, то есть 91% потока
 * управления пропадало, и по счётчикам это выглядело как потери в эфире.
 *
 * Поэтому write() только кладёт байты в очередь, а физическую посылку
 * ведёт отдельная задача. Она же склеивает всё накопленное в один разворот
 * линии: на одном проводе каждый разворот стоит куска встречного потока.
 *
 * Переполнение очереди означает, что провод медленнее источника. Тогда
 * лучше выбросить свежие байты и посчитать их, чем задержать поток
 * управления: устаревшие команды RC не нужны никому. */
#define UART_MGR_TX_BLOCK_BYTES  128
#define UART_MGR_TX_QUEUE_DEPTH  24
#define UART_MGR_TX_BATCH_BYTES  512

/* Однопроводный half-duplex: отвечать надо В ПАУЗЕ, а не когда вздумается.
 *
 * Пульт отдаёт кадры непрерывно — на 400000 бод это 250 кадров в секунду,
 * кадр занимает около 0.65 мс, остальные 3.35 мс линия свободна. Настоящий
 * радиомодуль отвечает именно в этот промежуток. Посылка «когда придётся»
 * попадает в середину чужого кадра, и оба сообщения гибнут: проверено на
 * стенде, ошибки приёма с пульта выросли с одной до десяти в секунду, а сам
 * пульт в итоге завис.
 *
 * Момент паузы задаёт РАЗБОР: кадр дособран — линия свободна, см.
 * uart_manager_notify_line_free(). По буферу драйвера это видно слишком
 * грубо, и обе попытки угадать оттуда (короткое чтение, пустой буфер)
 * ошибок не снизили: при опросе раз в 2 мс и кадре раз в 4 мс оба признака
 * истинны почти всегда, в том числе за миг до начала следующего кадра.
 *
 * Ждём сигнала не бесконечно: если встречный конец вообще не говорит
 * (дальняя сторона моста), синхронизировать не с чем и посылка уходит
 * сразу, иначе поток управления упёрся бы в этот таймаут. */

/* Сколько ждать возврата собственной посылки, прежде чем списать долг.
 * Эхо на общем проводе приходит сразу; всё, что не вернулось за это окно,
 * потеряно, и вычитать его из последующего приёма нельзя — это уже чужие
 * данные. */
#define UART_MGR_ECHO_DEBT_MS              5

#define UART_MGR_SINGLE_WIRE_GAP_WAIT_MS   2
#define UART_MGR_SINGLE_WIRE_GAP_LIMIT_MS  8
#define UART_MGR_PEER_QUIET_MS             200

/* Ограничители дампа. CRSF на 400000 бод даёт около 50 кадров в секунду,
 * и дамп каждого куска забил бы 4 КБ кольцевого буфера лога за секунду,
 * вытеснив оттуда всё остальное, да ещё и нагрузил бы форматирование.
 * Поэтому не чаще одной строки в 100 мс на канал и направление, а всё
 * пропущенное за это время учитывается и печатается в следующей строке —
 * дамп прорежен, но об этом честно сообщается. */
#define UART_MGR_DUMP_MIN_INTERVAL_MS  100
#define UART_MGR_DUMP_MAX_BYTES        32

/* Префикс UART_MGR_, а не UART_ — hal/uart_types.h из ESP-IDF уже
 * занимает имена UART_PARITY_EVEN/UART_PARITY_ODD. */
typedef enum {
    UART_MGR_PARITY_NONE = 0,
    UART_MGR_PARITY_EVEN,
    UART_MGR_PARITY_ODD,
} uart_mgr_parity_t;

typedef enum {
    UART_STOPBITS_1 = 0,
    UART_STOPBITS_1_5,
    UART_STOPBITS_2,
} uart_mgr_stopbits_t;

typedef enum {
    UART_DUPLEX_FULL = 0,
    UART_DUPLEX_HALF_SINGLE_WIRE,   /* один физический провод, TX==RX GPIO */
    UART_DUPLEX_HALF_RS485,         /* аппаратный RS-485 half-duplex + DE */
} uart_mgr_duplex_t;

/* Режим подключения канала CRSF. Их РОВНО ДВА, и путать их нельзя:
 * физика линии, направление обмена и допустимые настройки у них разные.
 *
 * CRSF_MODE_RX_ONLY_SPORT — пульт (RadioMaster TX16S) отдаёт CRSF по
 *   S.Port, плата только слушает. Провод один, но двунаправленным он НЕ
 *   является: TX платы физически никуда не подключён, и передавать в него
 *   нечего. Заводские значения: RX GPIO33, 400000 бод, 8N1, инверсия RX
 *   включена, инверсия TX выключена, дуплекс полный.
 *
 *   TX GPIO в конфигурации остаётся заданным — он нужен проверке пинов и
 *   не мешает, — но никуда не подключается. Мост работает строго в одну
 *   сторону: UART -> сеть. Это НЕ «двунаправленный CRSF по одному
 *   проводу», и называть его так нельзя.
 *
 *   Передача в этом режиме запрещена на уровне кода, а не соглашения:
 *   запись в провод пульта уже роняла TX16S, причём повторяемо.
 *
 * CRSF_MODE_SINGLE_WIRE — настоящая общая двунаправленная линия CRSF.
 *   RX GPIO и TX GPIO — это ОДИН И ТОТ ЖЕ вывод, что проверяется
 *   конфигурацией и не подменяется молча. Скорость настраивается (обычно
 *   400000 или 420000), 8N1, дуплекс half / single-wire. Для прямого
 *   CRSF TTL инверсия выключена; включать её нужно только при внешнем
 *   инверторе, и тогда она применяется к обоим направлениям сразу —
 *   провод один, разных инверсий у него быть не может.
 *
 * CRSF_MODE_FULL_DUPLEX — два ОТДЕЛЬНЫХ провода: приём на одном выводе,
 *   передача на другом. Подходит устройствам, у которых выводы приёма и
 *   передачи раздельные (например передатчик sine.link).
 *
 *   Это самый простой из трёх режимов и, где он применим, самый лучший.
 *   Общего провода нет, поэтому исчезают разом: арбитраж линии, ожидание
 *   межкадрового промежутка, собственное эхо и коллизии. Стоит это одного
 *   лишнего вывода.
 *
 *   Разница видна на измерениях. На общем проводе к тому же устройству
 *   ~25 КБ/с собственного эха полностью скрывали настоящие 188 Б/с
 *   телеметрии, и она выглядела редкими всплесками; на двух проводах это
 *   ровный поток 13 кадров в секунду при нуле ошибок синхронизации.
 *
 *   RX GPIO и TX GPIO обязаны РАЗЛИЧАТЬСЯ, дуплекс полный. Инверсия у
 *   каждого направления своя, но у устройства она, как правило, общая:
 *   если приём требует инверсии, то и передача тоже.
 *
 * КАК ВЫБИРАТЬ. Общий провод годится, пока стороны говорят по очереди, и
 * перестаёт годиться, когда одна занимает его почти непрерывно. Поток
 * управления CRSF — 250 кадров в секунду, это 16% времени линии, но
 * промежутки идут кусками по 3.3 мс, и встречный кадр должен попасть в
 * один из них. Измерено на одном и том же устройстве: на общем проводе
 * встречная телеметрия дала ОДИН кадр за минуту, на двух проводах — 13
 * кадров в секунду. Она не терялась — ей негде было пройти.
 *
 * Поэтому: двусторонний обмен с плотным потоком в одну сторону — только
 * CRSF_MODE_FULL_DUPLEX. Однопроводный режим берут тогда, когда общий
 * провод задан конструкцией устройства, а встречный поток редкий. */
/* Электрика вывода на общем проводе: чем формируется уровень и куда тянет
 * подтяжка. Вынесено в настройки, а не выбрано раз и навсегда, — и это
 * главный урок из исходников Betaflight.
 *
 * Там тип выхода и подтяжка выводятся из флагов порта
 * (src/main/drivers/serial_impl.c): serialOptions_pushPull() даёт двухтактный
 * при SERIAL_INVERTED или SERIAL_BIDIR_PP, serialOptions_pull() — подтяжку
 * вниз при SERIAL_INVERTED. Но само правило они НЕ прибивают: SERIAL_BIDIR_PP,
 * SERIAL_PULL_NONE, SERIAL_PULL_SMARTAUDIO задаются под конкретное железо.
 *
 * Проверено, почему одного правила мало. На проводе пульта TX16S с включённой
 * инверсией их комбинация (двухтактный + подтяжка вниз) дала 38% искажённого
 * эха против 45%, но испортила ПРИЁМ: ошибки CRC выросли с нуля до 83 за
 * пятнадцать секунд, кадров стало 245/с вместо 252. Флаг инверсии означает у
 * нас не то же, что у них: там это полярность линии, здесь — инверсия внутри
 * периферии ESP32, а уровень покоя задаёт встречное устройство.
 *
 * Значения по умолчанию — открытый сток и подтяжка вверх: на этой линии они
 * измеряются лучше всего (45% эха при нуле ошибок приёма). */
typedef enum {
    UART_MGR_PULL_UP = 0,
    UART_MGR_PULL_DOWN,
    UART_MGR_PULL_NONE,
} uart_mgr_pull_t;

typedef enum {
    CRSF_MODE_RX_ONLY_SPORT = 0,
    CRSF_MODE_SINGLE_WIRE   = 1,
    CRSF_MODE_FULL_DUPLEX   = 2,
} uart_mgr_crsf_mode_t;

typedef enum {
    PROTO_MODE_RAW = 0,
    PROTO_MODE_CRSF,
    PROTO_MODE_SBUS,
    PROTO_MODE_MAVLINK,
    PROTO_MODE_SPORT,
    PROTO_MODE_RS485_RAW,
    PROTO_MODE_SBUS_TO_CRSF,
    PROTO_MODE_CRSF_TO_MAVLINK,
    PROTO_MODE_CUSTOM,
    PROTO_MODE_MAX
} uart_mgr_protocol_t;

typedef struct {
    uint8_t  channel_id;                       /* 0=UART0/AUX,1=UART1/CRSF,2=UART2/MAVLink */
    char     name[UART_MGR_MAX_NAME_LEN];
    int      rx_gpio;
    int      tx_gpio;
    uint32_t baud_rate;                         /* 9600 .. 5000000 */
    uart_word_length_t data_bits;                /* UART_DATA_5_BITS..UART_DATA_8_BITS */
    uart_mgr_parity_t   parity;
    uart_mgr_stopbits_t stop_bits;
    bool     invert_rx;
    bool     invert_tx;
    uart_mgr_duplex_t duplex;
    int      rs485_de_gpio;                      /* -1 если не используется */
    int      rs485_re_gpio;                      /* -1 если /RE объединён с DE или не нужен */
    uint32_t rs485_turnaround_us;                /* задержка переключения направления */
    uart_mgr_protocol_t protocol;
    /* Смысл имеет только при protocol == PROTO_MODE_CRSF. Для остальных
     * протоколов поле не читается. */
    uart_mgr_crsf_mode_t crsf_mode;

    /* Электрика общего провода. Смысл имеют только в однопроводном режиме:
     * в полном дуплексе выход и так двухтактный, а вход не разворачивается.
     *
     * tx_push_pull — двухтактный выход вместо открытого стока. Он формирует
     *   ОБА фронта сам, не полагаясь на подтяжку, но при одновременной
     *   передаче обоих концов выходы будут бороться, а не совместно тянуть
     *   линию вниз. Включать там, где фронт вверх не успевает.
     * pull — куда тянуть вывод в покое. Должно совпадать с уровнем покоя
     *   ЛИНИИ, иначе подтяжка воюет с ней постоянно. */
    bool             tx_push_pull;
    uart_mgr_pull_t  pull;
    uint32_t rx_watchdog_timeout_ms;              /* 0 = отключено */
    bool     enabled;
} uart_mgr_channel_cfg_t;

/* Человекочитаемое имя режима — для UI, /api/status и лога. Одно и то же
 * имя во всех трёх местах: расхождение здесь уже приводило к тому, что
 * односторонний приём считали двунаправленным мостом. */
const char *uart_manager_crsf_mode_name(uart_mgr_crsf_mode_t m);

/* Колбэк, вызываемый при получении новых сырых байт от UART.
 * Вызывается из задачи канала (не ISR) — можно делать сетевые вызовы. */
typedef void (*uart_mgr_rx_cb_t)(uint8_t channel_id, const uint8_t *data, size_t len, void *user_ctx);

esp_err_t uart_manager_init(void);

/* Применить (пере)конфигурацию канала: останавливает драйвер если был
 * запущен, переинициализирует GPIO/параметры, запускает задачу заново. */
esp_err_t uart_manager_apply_config(const uart_mgr_channel_cfg_t *cfg);

esp_err_t uart_manager_get_config(uint8_t channel_id, uart_mgr_channel_cfg_t *out_cfg);

/* Подписка на приход байт с канала (используется routing_manager). */
esp_err_t uart_manager_register_rx_cb(uint8_t channel_id, uart_mgr_rx_cb_t cb, void *user_ctx);

/* Отправить байты в UART (используется routing_manager при приёме из сети). */
esp_err_t uart_manager_write(uint8_t channel_id, const uint8_t *data, size_t len);

/* «Кадр встречной стороны дособран — линия свободна».
 *
 * Зовётся разбором протокола, а не задачей приёма: только парсер знает, где
 * кончился кадр, а по буферу драйвера это видно слишком грубо. На одном
 * проводе это единственный момент, когда можно отвечать не поверх чужой
 * передачи. Для остальных режимов вызов ничего не делает. */
void uart_manager_notify_line_free(uint8_t channel_id);

/* Обновление watchdog по приёму данных / проверка таймаута (диагностика). */
bool uart_manager_is_channel_alive(uint8_t channel_id);

/* Счётчики для диагностики */
typedef struct {
    uint64_t rx_bytes;
    uint64_t tx_bytes;
    uint32_t rx_overruns;
    uint32_t last_rx_time_ms;
    /* Байты, выброшенные при переполнении очереди передачи: провод не
     * успевает за источником. Растущий счётчик — единственный внешний
     * признак того, что канал сузился, поэтому он виден в /api/status. */
    uint32_t tx_dropped;
    /* Попытки передать в канал, который передавать не имеет права
     * (CRSF_MODE_RX_ONLY_SPORT). Не ошибка транспорта, а признак
     * неверной настройки маршрутизации: кто-то шлёт из сети в пульт. */
    uint32_t tx_blocked;
} uart_mgr_stats_t;

esp_err_t uart_manager_get_stats(uint8_t channel_id, uart_mgr_stats_t *out_stats);
void uart_manager_reset_stats(uint8_t channel_id);

/* Заполняет структуру заводскими значениями по умолчанию (board_config.h) */
void uart_manager_default_config(uint8_t channel_id, uart_mgr_channel_cfg_t *out_cfg);

/* Побайтовый дамп трафика канала в лог (/api/log).
 *
 * Намеренно НЕ входит в app_config_t и не сохраняется в NVS: это
 * отладочный переключатель, после перезагрузки он всегда выключен.
 * Забытый включённым дамп иначе молча грузил бы лог в рабочем режиме. */
esp_err_t uart_manager_set_dump(uint8_t channel_id, bool enabled);
bool      uart_manager_get_dump(uint8_t channel_id);

/* Откуда взялись байты в дампе. Метки раздельные, потому что на общем
 * проводе перепутать их проще всего: своё эхо, чужой кадр и то, что пришло
 * из сети, выглядят в hex одинаково, а означают ровно противоположное.
 * Каждое направление прореживается независимо — иначе плотный поток RC не
 * даёт увидеть редкую посылку в обратную сторону. */
typedef enum {
    UART_DUMP_UART_RX = 0,   /* пришло с провода */
    UART_DUMP_UART_TX,       /* отдано в провод */
    UART_DUMP_UDP_RX,        /* пришло из сети */
    UART_DUMP_ECHO,          /* снято как собственное эхо */
    UART_DUMP_DIRS
} uart_mgr_dump_dir_t;

void uart_manager_dump(uint8_t channel_id, uart_mgr_dump_dir_t dir,
                       const uint8_t *data, size_t len);

#ifdef __cplusplus
}
#endif
